server:
  port: 8080
  servlet.context-path: /ms

spring:
  application.name: memo-service

  jpa:
    # Uncomment and run application to generate DB schema with Spring Data JPA
    #properties.javax.persistence.schema-generation:
    #  create-source: metadata
    #  scripts:
    #    action: create
    #    create-target: src/main/resources/db/migration/V1__create_schema.sql

    generate-ddl: false
    hibernate:ddl-auto: validate

    open-in-view: false

  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:ms
    username: sa
    password:

  h2.console:
    enabled: true
    path: /h2-console

springdoc.swagger-ui:
  tags-sorter: alpha
  operations-sorter: alpha

# Memo service (ms)
# ms: